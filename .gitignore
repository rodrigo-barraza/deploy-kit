# Node.js dependencies
node_modules/

# Deploy logs
.deploy-logs/

# Persistent deploy state (SHA markers for change detection)
.deploy-state/

# Deploy secrets (real values — use .env.deploy.example as template)
.env.deploy

# Test fixtures (separate repo checkouts)
test-components-lib/


# Migrated to pnpm
package-lock.json
edge/generated/
